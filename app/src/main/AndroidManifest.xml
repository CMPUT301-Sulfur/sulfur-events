<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <!--permissions-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SulfurEvents" >

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".OrganizerCreateEventActivity"
            android:exported="true"/>
        <activity
            android:name=".OrganizerActivity"
            android:exported="true" />

        <activity
            android:name=".AdminDashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".OrganizerWaitlistActivity"
            android:exported="false" />
        <activity
            android:name=".NotificationsActivity"
            android:exported="false" />

        <activity android:name=".OrganizerEnrolledActivity" android:exported="false"/>

        <activity android:name=".OrganizerInvitedActivity" android:exported="false"/>




        <!-- Other activities -->
        <activity android:name=".AdminEventsActivity" />
        <activity android:name=".AdminProfilesActivity" />
        <activity android:name=".AdminImagesActivity" />
        <activity android:name=".EventImageDetailActivity" />
        <activity android:name=".WelcomeEntrantActivity"/>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".UpdateProfileActivity"/>
        <activity android:name=".EntrantActivity" />
        <activity android:name=".OrganizerViewEventActivity" />
        <activity android:name=".LotteryGuidelinesActivity" />
        <activity android:name=".EventDetailsActivity" />
        <activity android:name=".OrganizerExportFinalListCSV"/>



        <!-- Keep AdminDashboardActivity declared even when commented above -->
        <!--        <activity android:name=".AdminDashboardActivity" android:exported="true" />-->

    </application>

</manifest>