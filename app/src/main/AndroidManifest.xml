<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SulfurEvents">

        <!-- Admin Dashboard is now the entry point -->
        <activity
            android:name=".AdminDashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other admin screens (no intent filter needed) -->
        <activity android:name=".AdminEventsActivity" />
        <activity android:name=".AdminProfilesActivity" />
        <activity android:name=".AdminImagesActivity" />
        <activity android:name=".EventImageDetailActivity" />

        <!-- Organizer and Main can stay for later use -->
        <activity android:name=".OrganizerActivity" />
        <activity android:name=".MainActivity" />

    </application>


</manifest>