<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SulfurEvents">

        <!-- ========================================================= -->
        <!-- 🚀 LAUNCHER ACTIVITY (Entry Point of the App)             -->
        <!-- ========================================================= -->
        <!-- Currently launching AdminDashboardActivity -->
        <!-- To switch the launcher, simply move the intent-filter -->
        <!-- block to MainActivity instead. -->
        <activity
            android:name=".WelcomeEntrantActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ========================================================= -->
        <!-- 👥 USER & PROFILE FLOWS                                   -->
        <!-- ========================================================= -->
        <activity android:name=".MainActivity" android:exported="true" />
<!--        <activity android:name=".WelcomeEntrantActivity" android:exported="true" />-->
        <activity android:name=".ProfileActivity" android:exported="true" />
        <activity android:name=".UpdateProfileActivity" android:exported="true" />

        <!-- ========================================================= -->
        <!-- 🧩 ADMIN SECTION                                           -->
        <!-- ========================================================= -->
        <activity android:name=".AdminEventsActivity" android:exported="true" />
        <activity android:name=".AdminProfilesActivity" android:exported="true" />
        <activity android:name=".AdminImagesActivity" android:exported="true" />
        <activity android:name=".EventImageDetailActivity" android:exported="true" />

        <!-- ========================================================= -->
        <!-- 🎟️ ORGANIZER SECTION                                      -->
        <!-- ========================================================= -->
        <activity android:name=".OrganizerActivity" android:exported="true" />
        <activity android:name=".OrganizerCreateEventActivity" android:exported="true" />
        <activity android:name=".LotteryGuidelinesActivity" android:exported="true" />

    </application>

</manifest>
